interface:
  display_name: "OpenIntent Review"
  short_description: "Review intent sources and compiled software"
  default_prompt: "Use $openintent-review to review this change against the repository's mapped intent."
