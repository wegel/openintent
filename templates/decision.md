# DEC-<NUMBER>: <Decision title>

| Field | Value |
| --- | --- |
| ID | `DEC-<NUMBER>` |
| Status | Draft |
| Date | `<YYYY-MM-DD>` |
| Product authority scopes | `<registry scopes>` |
| Accepted by | `<named approvers or pending>` |
| Affected intent | `<requirement IDs>` |
| Change | `<change ID>` |
| Review trigger | `<date, event, evidence, or none>` |

`Accepted` means the named approvers accepted this account of their choice and
its reasons. The linked product, capability, or quality rules define required
product behavior.

## Question

What durable product choice must future intent authors understand?

## Context

<!-- State concrete forces, actors, evidence, and constraints. Do not turn the
current architecture into a product need without an external reason. -->

<Context>

## Decision

`<Named decision owner>` chose `<product policy>` because `<reason>`.

<!-- Update the affected capability or quality rules in the same change. This
record cannot create product behavior by itself. -->

## Consequences

- `<Actor or future author>` gains `<benefit>`.
- `<Actor or future author>` accepts `<cost or constraint>`.
- `<Follow-up action and owner>`.

## Alternatives considered

### <Alternative>

- Rejected because: `<reason tied to product evidence or constraint>`
- Reconsider when: `<new fact that could change the choice>`

## Supporting evidence

- `<durable source link and what it supports>`

## Supersession

<!-- Fill when a later decision replaces this one. Keep this file as a stable
tombstone. -->

- Superseded by: `<decision ID or none>`
- Related requirement change: `<change ID or none>`
