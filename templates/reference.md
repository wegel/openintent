# <Normative supporting artifact title>

| Field | Value |
| --- | --- |
| ID | `REF-<NAME>` |
| Status | Draft |
| Product | `PROD-<NAME>` |
| Product authority scope | `<registry scope>` |
| Accepted change | `<change ID or pending>` |
| Governing intent IDs | `<requirement or invariant IDs>` |
| Source path | `<checked-in artifact path>` |
| Reviewable rendering or export | `<checked-in path or same as source>` |
| Format | `<SVG, PNG, HTML, JSON, audio, Markdown annex, or other>` |
| Last reviewed | `<YYYY-MM-DD>` |

## Why this medium belongs at the product boundary

`<Actor>` must perceive or exchange `<exact property>`. The supporting artifact
communicates `<visual, audible, spatial, temporal, or wire detail>` more clearly
than duplicated prose.

## Normative scope

The artifact requires:

- `<exact governed property>`

The artifact illustrates but does not require:

- `<sample content, device frame, annotation, or other non-normative detail>`

## Allowed variation

| Condition | Implementations may vary | Implementations must preserve |
| --- | --- | --- |
| `<platform, viewport, language, accessibility mode, or protocol version>` | `<permitted difference>` | `<governed property>` |

## Semantic contract

The governing text defines `<actors, states, permissions, failures,
accessibility meaning, or other behavior>`. This artifact does not add or alter
those duties.

## Acceptance

| Governing intent ID | Comparison conditions | Evidence needed |
| --- | --- | --- |
| `<intent ID>` | `<profile, platform, input, or state>` | `<inspection, render comparison, fixture replay, listening check, or other check>` |

